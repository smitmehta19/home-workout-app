// Bump on every deploy. Shown in Settings so the running build is identifiable
// — without it, "the fix isn't working" and "you're on an old cached build"
// look exactly the same from the outside.
export const APP_VERSION = '2026.08.18.5';
